pipeline {
    agent {
    	docker { image 'node:7-alpine' }
    }

    stages {

	    stage("Node install"){
	    	steps {
	    		sh "cd server && npm install"
	    	}

	    }

	   /* Uncommented until we actually have some tests 
	   stage("Test") {
	    	steps {
	    		sh "./main/tests.js"
	    	}
	    } 
	    */
}

}